{
  "name": "biometeric-api",
  "version": "1.0.0",
  "description": "api's for biometric",
  "main": "icefire.js",
  "scripts": {
    "start": "node bin/dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir dist && babel server -s -d dist",
    "production": "npm run build && node bin/prod"
  },
  "author": "Ashish",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cookie-parser": "^1.4.4",
    "debug": "^4.1.1",
    "express": "^4.16.4",
    "http-errors": "^1.7.2",
    "mongoose": "^5.4.22",
    "morgan": "^1.9.1",
    "babel-register": "^6.26.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  },
  "devDependencies": {
   
    
    
  }
}
